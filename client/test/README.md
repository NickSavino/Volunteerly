# Client Tests

This folder contains automated tests for client-side services, shared utilities, and frontend test setup used by the Volunteerly application.

These tests mainly cover:

- service wrappers that communicate with the backend API
- shared utility and navigation helper logic
- common fixtures, Jest environment setup, and reusable test helpers

Use this folder to validate frontend-facing business logic and API contract handling without needing to run the full UI manually.
